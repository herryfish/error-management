# 错题管理系统 · 需求确认书 v1.3（完整版）

| 项 | 内容 |
|----|------|
| **状态** | 待最终确认 |
| **版本** | v1.3（整合v1.1产品需求 + v1.3工程自动化） |
| **日期** | 2026-07-19 |
| **产品定位** | 高三学生 + 家长 自用的错题闭环工具（先自用，验证后再考虑上线与会员） |
| **相关文件** | `错题管理系统_需求确认书_v1.1.md`（产品细节全文） |

---

## 0. 版本说明

本文档整合了：
- **v1.1**：产品需求全文（角色、采集、重做、掌握、进度、LLM用量等）
- **v1.3增量**：工程自动化全流程（变更→审批→CI/CD→部署→追溯）
- **用户确认**：技术选型、算法参数、配置范围等

**AI Spike说明**：本流程跳过AI Spike执行，在其他环境执行；设计阶段保留降级开关。

---

## 1. 产品目标

帮助学生把错题从「拍下来就忘」变成完整闭环：

**采集 → 重做（在线 / 拍照手写）→ 不会则引导 → 掌握判定 → 间隔巩固 → 反复错则出相似题 → 家长全程可见进度与明细。**

一句话描述：

> 面向高三学生及其家长的错题管理与巩固系统：拍照入库、重做批改、引导解答、间隔掌握、相似题强化，家长透明监督，管理员可观测大模型消耗。

**工程自动化目标**：

> **整个系统的修改与部署能够完全自动化**——从提出修改需求，到审批（人**或**AI），到开发提交，再到上线，形成闭环流水线。

---

## 2. 角色与权限（V1）

| 角色 | 说明 |
|------|------|
| **学生** | 唯一学习主体：录入、重做、引导问答、今日任务、相似题、改判 |
| **家长** | 与学生 **1:1 绑定**（V1 仅一个家长对应一个学生）；**完全透明**只读查看 |
| **管理员** | 后台查看大模型消耗明细与汇总（可与部署者本人账号合一） |
| **老师** | V1 **不做** |
| **一家长多学生** | V1 **不做** |

### 2.1 家长可见范围（完全透明 + 只读）

家长**可以**查看：

- 每道错题的对错结果
- 作答用时
- 是否查看过完整解析
- 是否进入「不会 / 引导模式」
- 收录量、重做次数、掌握率、薄弱知识点
- 周报：上周薄弱知识点 + 相似题相关情况
- 站内红点提醒

家长**不可以**：

- 改判（对/错结果由学生侧可改，家长只读）
- 替学生作答或修改题干（除非后续另定）

> 说明：V1 优先「监督透明度」。若日后学生抵触，可再增加「可见级别」开关。

### 2.2 学生权限摘要

- 录入并确认错题
- 在线作答 / 拍照手写作答
- 进入引导模式；在配置允许时「直接看解析」
- **可改判**批改结果（需记录审计日志）
- 对相似题标记「不适用」并重新生成
- 与家长共同设定学习目标（题量等）

---

## 3. 学科与内容范围

| 项 | 确认 |
|----|------|
| 首发学科 | **数学、物理、化学** |
| 题目呈现 | **识别后的文本 + 公式为主**，原图保留作对照 |
| 手写批改 | **必须**：支持拍照上传手写解题过程并批改 |
| 整页多题切分 | V1 **不做**；**一次拍一题** |
| 扩展性 | 数据结构按全科可扩展预留；V1 产品与提示词聚焦数理化 |

---

## 4. 功能需求

### 4.1 错题采集

1. 手机拍照或上传图片（**一题一图**）。
2. 大模型（多模态）分析，抽取并结构化，例如：
   - 科目、题型
   - 题干文本（含公式）、条件与设问
   - 图中若有：答案、解析
   - 知识点标签、难度估计（可编辑）
3. **学生确认后入库**（识别结果可编辑，避免脏数据污染错题本）。
4. 原图永久关联保存，作答与批改时可对照。
5. 识别失败 / 低置信度时提示重拍或手改。

### 4.2 重做

| 方式 | 要求 |
|------|------|
| 在线解答 | 文本作答（按题型适配填空/简答等） |
| 拍照解答 | **必须**：拍手写过程 → 模型识别过程与结果 → 批改 |

批改原则：

- 能自动判的尽量自动判。
- 主观题 / 过程题：模型给出「对 / 部分对 / 错」及简要依据。
- **学生可改判**；家长只读结果。
- 批改结果进入掌握状态机。
- **改判需记录审计日志**：时间、用户、题目、前后状态。

### 4.3 引导解答与「直接看解析」

| 模式 | 行为 |
|------|------|
| **默认（后台开启「直接看解析」开关时）** | 学生可随时点「直接看解析」；该题走薄弱 / 未掌握路径并记录 |
| **后台关闭该开关时** | 严格引导：只提问、给思路台阶，**不直接给最终答案** |

补充：

- 引导为多轮问答，推动学生自己写出关键步骤。
- 无法独立完成的题需明确 **标识**（如：不会 / 依赖解析 / 反复错）。
- **全局配置**：后台可配置是否展示「直接看解析」。

### 4.4 掌握与巩固

| 规则 | 确认 |
|------|------|
| 掌握条件 | **连续 2 次独立做对**（未依赖完整解析） |
| 固定间隔 | **1天、7天、30天**（第一次做对后间隔1天，第二次做对后间隔7天，第三次做对后间隔30天） |
| 看完完整解析 | **重置为未掌握**，重新进入巩固队列 |
| 每日学习量 | **系统智能推荐 + 学生/家长设定目标**（结合） |

队列概念（设计阶段细化算法）：

- 今日待重做（新错题）
- 今日巩固（到期复习）
- 薄弱 / 反复错
- 相似题练习

### 4.5 相似题

- 对 **连续错2次** 的题型 / 知识点：由 AI **生成**相似题。
- 学生可标记「不适用」并 **重新生成**。
- V1 **不做**外部标准题库对接。
- 生成可用相似题后，应能体现在家长可见的进度/周报中，并触发站内提醒（红点）。

### 4.6 进度、周报与提醒

| 对象 | 内容 |
|------|------|
| 学生 | 今日任务、掌握进度、薄弱点、错题状态 |
| 家长 | 完全透明明细；打开 H5 **即见周报入口**；**站内红点**；**不做短信** |
| 周报节奏 | **每周一** 可看到 **上周** 薄弱点报告及相关提醒（含相似题情况） |

### 4.7 账号与登录

| 项 | 确认 |
|----|------|
| 登录方式 | **用户名 + 密码** |
| 绑定关系 | 一个学生账号 ↔ 一个家长账号（V1） |

### 4.8 大模型用量可观测（后台 · V1 必做）

**目标：** 凡调用大模型的能力，均需在后台可查询消耗，便于自用控本，并为今后「免费额度 + 会员」留扩展。

#### 建议统计的调用场景

| 场景 | 说明 |
|------|------|
| 错题识别 / 结构化 | 拍照识题 |
| 手写过程批改 | 拍照解答批改 |
| 引导问答 | 多轮对话（按请求/轮次记账） |
| 完整解析生成（若发生） | 看解析时若需模型生成 |
| 相似题生成 | 含重新生成 |
| 其他 LLM 调用 | 一律记账，避免漏计 |

#### 后台最低能力

1. **用量明细**
   - 时间、用户（学生）、场景类型、模型名
   - Token（input / output / total，若接口提供）
   - 估算费用（可选：单价可配置）
   - 成功 / 失败、耗时
   - 关联业务 ID（错题 ID、会话 ID 等）

2. **汇总视图**
   - 按日 / 周 / 月
   - 按场景、按模型、按用户
   - 总调用次数、总 Token、失败率

3. **访问控制**
   - 管理员可访问
   - 学生 / 家长端 **不展示** 费用与 Token 明细

4. **实现原则**
   - 每次 LLM 请求均落库（失败也记一条）
   - 预留字段：`provider`、`model`、`scene`、`tokens_*`、`cost`、`request_id` 等
   - V1 **不做** 扣费拦截与支付；仅为今后额度制预留结构

---

## 5. 非目标（V1 明确不做）

- 老师端 / 班级 / 布置作业
- 一家长绑定多学生
- 微信小程序 / 原生 App（后期再考虑）
- 支付、会员、免费额度扣减（架构可预留，V1 不实现）
- 短信 / 微信模板消息等外部推送
- 整页多题自动切分
- 外部标准题库对接
- 公网正式运营所需的完整合规与内容审核流水线（自用优先）
- 语数外全科首发、英语听力等
- 社区、排行榜、教辅整书导入

---

## 6. 成功标准（MVP 验收）

在真实自用场景下，满足：

1. 一名高三生 **一周内录入 ≥ 20 道** 错题。
2. 完成 **≥ 15 次** 重做（含在线或拍照手写）。
3. 家长在 **周一** 能看到 **上周薄弱知识点**。
4. 对标记为反复错的知识点，系统能 **生成可用相似题**。
5. 能 **提醒家长**（H5 周报 + 站内红点；无短信）。
6. （建议）连续使用一周后，管理员能在后台看到 **各场景 Token/次数汇总**，并与实际调用大致可对上。

**工程成功标准**（与产品成功标准并列）：

1. 一次「低风险变更」从创建变更单到生产可见，**无需 SSH 上手改业务文件**。
2. 任意生产部署可追溯到：变更说明、审批记录、Git SHA。
3. CI 失败则 **不能** 部署成功。
4. 模拟部署失败或主动回滚，可在约定时间内恢复上一版本。
5. 高风险变更在未人工批准时 **不能** 上线（门禁可测）。

---

## 7. 产品形态与运行环境

| 项 | 确认 |
|----|------|
| 客户端 | **H5**（移动浏览器优先；后期再考虑其他平台） |
| 部署 | **自家服务器**，使用 **Docker Compose** |
| CI/CD | **GitHub Actions** |
| 使用范围 | 暂时自用；效果好再考虑上线 |
| 商业化 | V1 不考虑付费；今后方向为 **免费额度 + 会员** |
| 数据库 | **MariaDB** |
| 技术原则 | 简洁、易懂、利于高效迭代 |

**技术栈**：
- 前端：H5（移动优先 Web 应用）
- 后端：自建服务器（具体框架待设计阶段确定）
- 数据库：MariaDB
- LLM 集成：支持多提供商配置，允许配置降级模型（如OpenAI GPT-4V主模型，Claude 3降级模型），用于相似题生成和用量监控
- 部署：Docker Compose on 自家服务器，通过 GitHub Actions + CI/CD 自动化

---

## 8. 掌握状态机（需求级）

```
录入(待确认) → 已入库(待重做)
       ↓
   重做中 ──做对──→ 待第一次巩固(间隔1天) ──再做对──→ 待第二次巩固(间隔7天) ──再做对──→ 待第三次巩固(间隔30天) ──再做对──→ 已掌握
       │                      ↑                              ↑                              ↑
       │做错 / 不会            │                              │                              │
       ↓                      │                              │                              │
  引导模式 ←──────────────────┘（巩固失败等）                 │                              │
       │                                                   │                              │
       ├─ 引导后独立做对 → 进入巩固队列                       │                              │
       ├─ 直接看解析 / 仍不会 → 标记薄弱，重置未掌握           │                              │
       └─ 反复错同一知识点（连续错2次） → 生成相似题 + 家长站内提醒 │                              │
                                                           │                              │
                                                           └──────────────────────────────┘
```

**掌握条件**：连续3次独立做对（未依赖完整解析），间隔分别为1天、7天、30天。
**重置条件**：看完完整解析 → 重置为未掌握，重新进入巩固队列。

---

## 9. 用户故事（摘要）

1. 作为学生，我拍照上传一道错题，确认 AI 识别结果后入库。
2. 作为学生，我在线作答或拍照手写重做，得到批改结果；必要时我可以改判（系统记录审计日志）。
3. 作为学生，不会时获得引导式提问；在后台允许时也可直接看解析（并被标记）。
4. 作为学生，系统按固定间隔（1天、7天、30天）安排巩固；看过完整解析后掌握状态重置。
5. 作为学生/家长，我们可设定目标题量，系统也智能推荐今日任务。
6. 作为系统，对连续错2次的知识点生成相似题；学生可重生成。
7. 作为家长，我打开 H5 能看到周报与红点，并能查看全部学习明细（只读）。
8. 作为管理员，我能在后台查看大模型各场景的调用次数与 Token 消耗。
9. 作为开发者，我通过 Issue 提交变更，经审批后由 CI/CD 自动部署到生产环境。

---

## 10. 工程自动化需求（V1.3新增）

### 10.1 目标流水线（逻辑阶段）

```
① 提出变更（需求/缺陷/优化）
        ↓
② 审批（人工 和/或 AI 评审）
        ↓  通过
③ 自动/半自动开发（实现、自测、提交）
        ↓
④ CI（构建、检查、测试）
        ↓
⑤ 自动部署到运行环境（Docker Compose）
        ↓
⑥ 回执与可追溯（谁提的、谁批的、哪次提交、何时上线）
```

### 10.2 变更类型与审批策略

| 类型 | 示例 | 默认审批 |
|------|------|----------|
| 低风险 | 文案、样式、配置项（非密钥）、文档 | AI 审批可通过 |
| 中风险 | 一般功能、接口、表结构可逆迁移 | AI 初审 + 可选人工 |
| **高风险** | **数据库迁移、权限变更、部署脚本** | **必须人工审批** |
| 紧急热修 | 生产故障 | 人工快速通道 + 事后补单 |

### 10.3 流水线必须具备的能力

1. **单一事实来源**
   - 代码：Git 仓库（GitHub）
   - 变更：Issue/变更单与提交、部署记录可互链

2. **审批门禁**
   - 未审批不得合并受保护分支 / 不得部署生产
   - 审批结果（人/AI）、意见、时间落库或写回 Issue

3. **CI 门禁**（GitHub Actions）
   - 安装依赖、构建前端/后端
   - 静态检查 / 单元或关键路径测试（范围随项目增长）
   - 迁移脚本校验（MariaDB）

4. **CD 部署**
   - 面向 **自家服务器**，使用 **Docker Compose**
   - 密钥与 `.env` **不进仓库**；由服务器侧或密钥管理注入
   - 部署步骤幂等；健康检查；失败告警

5. **可观测与审计**
   - 每次上线：版本号/Git SHA、时间、触发的变更单、审批人（或 AI）
   - 与业务侧「LLM 用量审计」分离，但同一管理员入口可链到「发布历史」

### 10.4 V1 范围 vs 非目标（工程）

**V1 要做：**

- Git 托管（GitHub）
- 变更单模板 + 审批状态机（人 / AI 两种执行器）
- CI + 自动部署到自有服务器（Docker Compose）
- 发布记录与一键/自动回滚
- 受保护主分支策略

**V1 可不做（可列为 V1.1+）：**

- 多套完整环境（dev/stage/prod）的复杂晋升（自用可先 **单生产环境 + 可选预发**）
- 多租户 SaaS 发布
- 完整 GitOps 控制台产品化
- 无人工的「AI 永久免审」所有变更（高风险仍建议人审）

---

## 11. 技术约束与原则

| 项 | 确认 |
|----|------|
| 客户端 | H5 |
| 数据库 | MariaDB |
| 部署目标 | 自家服务器（Docker Compose） |
| CI/CD | GitHub Actions |
| 交付方式 | **Git + CI/CD 自动化**，禁止常规手工部署业务代码 |
| 变更治理 | 需求 → 审批（人/AI）→ 开发提交 → 上线可追溯 |
| 迭代 | 简洁技术栈，利于自动化与回滚 |
| LLM降级 | 设计需保留识别失败、手写批改、引导降级的配置开关，支持多提供商配置和自动降级策略 |
| 数据迁移 | 不可逆迁移需高风险审批（人工） |

---

## 12. 决策记录

| 编号 | 议题 | 结论 |
|------|------|------|
| A-1 | V1 用户角色 | 仅学生 + 家长（1:1） |
| A-2 | 家长可见范围 | 完全透明（对错、用时、是否看解析等） |
| A-3 | 一家长多学生 | V1 不做 |
| B-4 | 首发学科 | 数理化 |
| B-5 | 题目呈现 | 文本+公式为主，原图为辅 |
| B-6 | 手写拍照批改 | 必须 |
| C-7 | 掌握定义 | 连续 3 次独立做对，固定间隔 1天、7天、30天；看解析重置未掌握 |
| C-8 | 引导与看解析 | 全局配置，默认可直接看解析；后台可关，关闭后仅严格引导 |
| C-9 | 每日题量 | 智能推荐 + 人工目标 |
| C-10 | 相似题 | AI 生成，连续错2次触发，可丢弃/重生成 |
| D-11 | 端形态 | H5 优先 |
| D-12 | 部署 | 自家服务器（Docker Compose） |
| D-13 | CI/CD | GitHub Actions |
| D-14 | 商业化 | V1 自用；今后免费额度+会员 |
| D-15 | 产品阶段 | 自用验证后再考虑上线 |
| E-16 | 成功标准 | 周录入≥20、重做≥15、周一周报薄弱点、相似题+家长提醒 |
| ENG-1 | 部署方式 | 全流程自动化（变更→审批→开发提交→上线） |
| ENG-2 | 审批主体 | 支持 **人工** 与 **AI**；高风险强制人工（数据库迁移、权限变更、部署脚本） |
| ENG-3 | AI Spike | 本流程跳过，他处执行 |
| ENG-4 | 需求基线 | v1.1 产品 + v1.3 工程自动化 |
| Q1 | 登录 | 用户名+密码 |
| Q2 | 提醒形态 | H5 周报 + 站内红点；不做短信 |
| Q3 | 整页切题 | V1 不做，一次一题 |
| Q4 | 改判 | 学生可改（需审计日志）；家长只读 |
| — | 数据库 | MariaDB |
| — | LLM 用量 | 后台明细+汇总，管理员可见 |
| LLM-1 | LLM配置系统 | 支持多提供商配置，允许配置主模型和降级模型 |
| LLM-2 | 降级策略 | 主模型失败时自动切换降级模型，可配置重试次数和超时时间 |

---

## 13. 确认栏

请审阅本文后回复：

- [ ] **确认 v1.3 完整版，进入设计** — 按本文启动系统设计（架构、MariaDB 表结构、LLM 用量方案、H5 信息架构、CI/CD 流水线、PR Plan），**暂不写业务代码**
- [ ] **确认，但需修改** — 请列出修改点：

```
修改点：
1.
2.
```

---

## 14. 下一步（确认后）

1. `/design`：产出系统设计文档与 PR Plan（包含LLM配置系统设计）
2. 设计评审收敛（0 open issues）
3. 你确认设计后再 `/execute-plan` 或分阶段实现

---

*本文档由需求访谈整理，版本 v1.3（完整版）。确认前不作为开发开工依据；确认后作为设计与验收的基准。*